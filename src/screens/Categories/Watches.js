import { View, Text } from "react-native";
import React from "react";

const Watches = () => {
  return (
    <View>
      <Text>Watches</Text>
    </View>
  );
};

export default Watches;
